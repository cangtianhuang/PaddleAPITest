# yaml-language-server: $schema=../run_config.schema.json
# 通用 1M/大 tensor paddle_only 配置：切换 ${APITEST_MODEL} 即可切换测试模型。
# 依赖 CI 提前将 ${APITEST_MODEL}/paddleonly/1M.txt 拷贝到 PaddleAPITest 下。
name: run_1M_paddleonly
runner:
  engine: engineV4
  foreground: true
env:
  FLAGS_use_system_allocator: "true"
  FLAGS_check_cuda_error: "true"
  FLAGS_alloc_fill_value: "255"
  FLAGS_check_nan_inf: "true"
input:
  api_config_file: "${APITEST_MODEL}/paddleonly/1M.txt"
output:
  log_dir: "test_${APITEST_MODEL}_log_1M_paddleonly"
engine_args:
  paddle_only: true
  use_gpu_mode: true
  num_gpus: -1
  num_workers_per_gpu: 1
  timeout: 1200
  use_compute_sanitizer: true
